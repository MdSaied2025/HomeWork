/* Question 10
Explain what happens if you try to use a variable before it is declared in Dart. */

// Dart reports a compile error if you try to use it before it has been definitely initialized.

void main() {
  StudentaAge = 14;
}
